id = "C09"
title = "The lock that deadlocks tomorrow"
part = "C"
status = "draft"

# What the lesson is written against. A kernel claim with no configuration attached is not a
# claim, so this block is not optional and refcheck reads it.
kernel = "7.2.2"
arch = "x86"
profile = "A-full"

# Tier 0 is the browser kernel under v86. Uniprocessor, 32 bit, emulated timing, which is exactly
# the machine on which a timing bug never reproduces and lockdep still finds the ordering.
tier = 0

# Why this is still a draft. Nothing technical is missing any more. All five claims are verified:
# three against the pinned tree, and two off one boot of the D-lockdep profile with abba.ko loaded
# into it, which produced the report in corpora/oops/tier0/ and the two readings of lockdep_stats
# in corpora/proc/tier0/. What is left is a person.
blocked_on = "review"
