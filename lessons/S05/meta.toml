id = "S05"
title = "The first ops plug"
part = "S"
status = "draft"

# What the lesson is written against. A kernel claim with no configuration attached is not a
# claim, so this block is not optional and refcheck reads it.
kernel = "7.2.2"
arch = "x86"
profile = "A-full"

# Tier 0 is the browser kernel under v86. Uniprocessor, 32 bit, emulated timing.
tier = 0

# Why this is still a draft. It is not the evidence any more. Four of the six claims are verified,
# and the other two are observations the reader makes on their own machine, which work today and
# have nothing left for anybody here to do. What is left is `reviewed-by`, which names a person
# who has read the lesson, and that is not a check a script can pass.
blocked_on = "review"
