id = "S05"
title = "The first ops plug"
part = "S"
status = "draft"

# What the lesson is written against. A kernel claim with no configuration attached is not a
# claim, so this block is not optional and refcheck reads it.
kernel = "7.2.2"
arch = "x86"
profile = "A-full"

# Tier 0 is the browser kernel under v86. Uniprocessor, 32 bit, emulated timing.
tier = 0

# Why this is still a draft. Two of the six claims are observations the reader makes on their own
# machine and those work today. The three source claims need a kernel tree to confirm against, and
# the trace claim needs a capture, which needs kxbox to boot.
blocked_on = "kxbox"
