id = "Z02"
title = "Your first trace"
part = "Z"
status = "draft"

# What the lesson is written against. A kernel claim with no configuration attached is not a
# claim, so this block is not optional and refcheck will read it once it exists.
kernel = "7.2.2"
arch = "x86"
config = "kxbox/kernel/teaching.config"

# Tier 0 is the browser kernel under v86. Uniprocessor, 32 bit, emulated timing.
tier = 0

# Why this is still a draft. The lesson is written, the claims are registered, and none of them
# are verified because there is no way to capture a real trace until kxbox boots.
blocked_on = "kxbox"
